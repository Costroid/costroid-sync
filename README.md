# costroid-sync

Open-source CLI for tracking, forecasting, and alerting on AI/LLM spend across providers — metadata only, zero credential storage.

Status: scaffolding (C1). First working provider lands in C2.

## Build

```
go build -o costroid-sync .
./costroid-sync --version
```
